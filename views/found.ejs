<!-- <!DOCTYPE html>
<h
</html> -->

















<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Search Results</title>
  <link rel="stylesheet" href="style/index.css">
</head>
<body>
  <header>📚 Book Finder</header>

  <section class="view-all-books">
    <h1>🔍 Search Results</h1>

    <% if (typeof error !== "undefined" && error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <% if (book && book.length > 0) { %>
      <div class="book-list">
        <% book.forEach(b => { %>
          <div class="book-card">
            <% if (b.isbn) { %>
              <img src="https://covers.openlibrary.org/b/isbn/<%= b.isbn %>-M.jpg" alt="<%= b.title %>">
            <% } else { %>
              <div class="no-cover">No cover</div>
            <% } %>

            <h2><%= b.title %></h2>
            <p><strong>Author:</strong> <%= b.author %></p>

            <% if (b.rating) { %>
              <p class="rating"><%= '★'.repeat(b.rating) %><%= '☆'.repeat(5 - b.rating) %></p>
            <% } %>

            <a href="/book/<%= b.id %>" class="btn">View More</a>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p class="empty">No books matched your search. Try again 🔎</p>
    <% } %>
     <br>
    <a href="/" style="display:inline-block; margin-top:20px; background:#fff; color:var(--primary); padding:10px 20px; border-radius:8px; text-decoration:none; font-weight:600;">⬅ Go Back</a>
  </section>
</body>
</html>
