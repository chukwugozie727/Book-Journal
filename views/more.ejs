<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= book ? book.title : "Book Details" %></title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f9fbff;
      margin: 0;
      padding: 20px;
      color: #333;
    }

    .book-details {
      max-width: 700px;
      margin: 0 auto;
      background: #fff;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      text-align: center;
    }

    .book-details img {
      max-width: 200px;
      height: auto;
      border-radius: 8px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      margin-bottom: 15px;
    }

    h1 {
      font-size: 1.8rem;
      color: #004aad;
      margin-bottom: 10px;
    }

    p {
      margin: 8px 0;
      font-size: 1rem;
    }

    .rating {
      color: #ffcc00;
      font-size: 1.2rem;
      margin: 10px 0;
    }

    .back-btn {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      padding: 10px 18px;
      background: #004aad;
      color: white;
      font-size: 1rem;
      border-radius: 8px;
      transition: background 0.3s ease;
    }

    .back-btn:hover {
      background: #0066ff;
    }

    .error {
      color: red;
      text-align: center;
      font-weight: bold;
    }

    /* Responsive */
    @media (max-width: 600px) {
      body {
        padding: 10px;
      }

      .book-details {
        padding: 15px;
      }

      h1 {
        font-size: 1.5rem;
      }

      p {
        font-size: 0.95rem;
      }

      .back-btn {
        width: 100%;
        display: block;
        text-align: center;
      }
    }
  </style>
</head>
<body>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } else if (book) { %>
    <div class="book-details">
      <% if (book.isbn) { %>
        <img src="https://covers.openlibrary.org/b/isbn/<%= book.isbn %>-L.jpg" alt="<%= book.title %>">
      <% } else { %>
        <div style="width:200px; height:280px; background:#eee; display:flex; align-items:center; justify-content:center; border-radius:8px; margin:0 auto 15px;">No Cover</div>
      <% } %>

      <h1><%= book.title %></h1>
      <p><strong>Author:</strong> <%= book.author %></p>

      <% if (book.rating) { %>
        <p class="rating"><%= '★'.repeat(book.rating) %><%= '☆'.repeat(5 - book.rating) %></p>
      <% } %>

      <p><strong>Date Read:</strong> 
        <%= book.date_read ? new Date(book.date_read).toISOString().split('T')[0] : "Not set" %>
      </p>

      <p><strong>Description:</strong> <%= book.description || "No description available." %></p>
      <p><strong>Notes:</strong> <%= book.notes || "No notes." %></p>

      <a href="/" class="back-btn">← Back to Books</a>
    </div>
  <% } %>

</body>
</html>
